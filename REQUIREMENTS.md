# REQUIREMENTS.md — Genpact Design System Integration

> **The previous contents are retired.** They encoded the old one-way Figma→Storybook,
> per-component-wrapper model (two-file rule, `const t` token blocks, per-component branding),
> which conflicts with the v2 round-trip's global-theme approach.
>
> See `docs/adr/0001-code-to-figma-round-trip.md` for the current direction and `CONTEXT.md`
> for canonical terms. New quality standards for the round-trip will be authored here when
> `/kendo-storybook-scaffold`, `/kendo-to-figma`, and `/figma-to-kendo` are built.

_(Retired contents recoverable from git history.)_
